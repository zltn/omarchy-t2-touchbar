[Service]
# ProtectSystem=strict mounts everything read-only, and sending to a unix
# socket needs write permission on its inode. Without this the slider is
# silently inert.
ReadWritePaths=/run/tiny-dfr
